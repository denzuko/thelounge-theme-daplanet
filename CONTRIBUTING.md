# Contributing

## Workflow
1. Open GitHub Issue
2. Branch: `feat/N` or `fix/N`
3. Write tests first
4. Update `CHANGELOG.md` under [Unreleased]
5. Push → PR → review → merge → semver tag

## Semver
- MAJOR — breaking public API change
- MINOR — new non-breaking capability
- PATCH — everything else
